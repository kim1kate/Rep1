#pragma once

int MultBy2(int value_);

/*
* @brief проверяет число x на простату 
* @param x число,которое мы проверяем
* return 1, если функция верна, 0,если функция неверна 
*/
long long int Prime(unsigned long long int x);